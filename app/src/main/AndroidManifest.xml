<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
package="com.mms.mashers">

<application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="TEE FLIP"
    android:supportsRtl="true"
    android:theme="@style/Theme.Mashers"
    tools:targetApi="31">

    <activity android:name=".activities.MainActivity" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".activities.HandicapCalculatorActivity"
        android:theme="@style/Theme.Mashers" />
    <activity android:name=".activities.HandicapTrackerActivity"
        android:theme="@style/Theme.Mashers" />
    <activity android:name=".activities.HandicapHistoryActivity"
        android:theme="@style/Theme.Mashers" />

</application>

</manifest>